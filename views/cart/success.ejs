<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head.ejs') %>
</head>
<body> 
  <%- include('../partials/nav.ejs') %>
  
  <div class="container mt-5">
    <div class="row mt-5">
        <div class="col-lg-12 col-sm-12 m-auto" >
            <div class="success_cart">
                <div style="display: flex;justify-content: space-between; align-items: center;">
                    <span style="font-size: 52px;">Transakcja zakończona sukcesem</span>
                    <i class="bi bi-check-circle-fill success_auth"></i>
                </div>
                <div class="d-flex">
                    <span style="font-size: 20px;text-indent :1em;">Numer zamównienia : <strong><%= orderId %></strong></span>
                </div>
                <div style="display: flex;justify-content: center; gap:10px" class="mt-5">
                    <a href="/" class="btn btn-primary">Strona główna</a> 
                    <% if(logged){ %>
                    <a href="/user/orders/<%= orderId %>" class="btn btn-success">Podgląd zamówienia</a>
                    <% } %>
                </div>
            </div>

            
        </div>
    </div>
  </div>


  <%- include('../partials/bootstrap.ejs') %>
</body>
</html>